

<h1> Products </h1>

<div>
<%= link_to "Create a Product", new_product_path %>
</div>

<div>
	<%= link_to "Log In", new_session_path %>
</div>

<% @products.each do |product| %>
	<p> <%= link_to image_tag(product), product_url(product.id) %></p>
<% end %>
